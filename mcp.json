{
  "mcpServers": {
    "example-server": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-example"
      ],
      "enabled": false,
      "description": "Example MCP server (disabled by default)"
    }
  }
}